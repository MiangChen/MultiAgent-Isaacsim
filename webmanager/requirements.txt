# WebManager Dependencies
# Core web framework and server
fastapi>=0.104.0,<1.0.0
uvicorn[standard]>=0.24.0,<1.0.0
websockets>=12.0,<13.0
python-multipart>=0.0.6

# Data processing and scientific computing
numpy>=1.24.0,<2.0.0  # Compatible with Isaac Sim
opencv-python>=4.8.0,<5.0.0
Pillow>=10.0.0,<11.0.0

# System monitoring and performance
psutil>=5.9.0,<6.0.0

# Async and threading support
asyncio-mqtt>=0.13.0,<1.0.0  # For potential MQTT integration

# Data validation and serialization
pydantic>=2.0.0,<3.0.0

# Logging and utilities
python-json-logger>=2.0.0,<3.0.0

# Chart and data visualization (server-side generation if needed)
matplotlib>=3.7.0,<4.0.0

# Optional ROS2 dependencies (commented out by default)
# Uncomment these if ROS2 integration is needed
# rclpy>=3.3.0
# ros2cli>=0.18.0
# ros2topic>=0.18.0
# ros2node>=0.18.0
# geometry-msgs>=4.2.0
# sensor-msgs>=4.2.0
# std-msgs>=4.2.0

# Development and testing dependencies (optional)
# pytest>=7.0.0,<8.0.0
# pytest-asyncio>=0.21.0,<1.0.0
# pytest-mock>=3.10.0,<4.0.0
# black>=23.0.0,<24.0.0
# flake8>=6.0.0,<7.0.0
# mypy>=1.0.0,<2.0.0

# Optional performance and optimization packages
# ujson>=5.7.0,<6.0.0  # Faster JSON parsing
# orjson>=3.8.0,<4.0.0  # Even faster JSON serialization
# python-jose[cryptography]>=3.3.0,<4.0.0  # For JWT tokens if authentication is added